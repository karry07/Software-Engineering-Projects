<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.Job_Seeker_CommentsDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.Job_Seeker_CommentsEntity" id="job_seeker_commentsMap">
        <result property="refid" column="refid"/>
        <result property="content" column="content"/>
        <result property="userid" column="userid"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.Job_Seeker_CommentsVO" >
		SELECT * FROM job_seeker_comments  job_seeker_comments
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.Job_Seeker_CommentsVO" >
		SELECT  job_seeker_comments.* FROM job_seeker_comments  job_seeker_comments
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.Job_Seeker_CommentsView" >

		SELECT  job_seeker_comments.* FROM job_seeker_comments  job_seeker_comments
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.Job_Seeker_CommentsView" >
		SELECT * FROM job_seeker_comments  job_seeker_comments <where> 1=1 ${ew.sqlSegment}</where>
	</select>
</mapper>